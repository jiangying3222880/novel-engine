# Novel Engine v1.4 · 检索运行时依赖
# 唯一 Python 部件。其余保持纯 Markdown。

# ===== 纯 py 核心依赖（已离线 vendored 到 runtime/deps，clone 即用，无需安装）=====
# jieba / pyyaml / pypinyin 已随仓库打包于 runtime/deps/，运行时通过 sys.path 自动加载。
# 如需手动升级：pip install --target runtime/deps --no-deps jieba pyyaml pypinyin

# ===== 可选增强：ZVEC 检索（BM25 + 实验性词袋向量混合；编译型 wheel，平台绑定）=====
# 未安装时 index.py/query.py 自动回退 BM25+FTS（真回退，见源码）。
# 当前 dense 向量为 jieba 词袋向量（实验性，非语义 embedding）；bge-small-zh 为预留项未接入。
zvec>=0.7.0

# 说明：numpy / sentence-transformers 已弃用
#   - numpy：index.py 中为死导入，已移除（无需安装）。
#   - sentence-transformers：被 Windows 长路径（WinError 206）阻断，装不上；
#     bge-small-zh-v1.5 为升级路径预留，勿启用。当前 embedding 用 index._embed（jieba TF-IDF 词袋，零重依赖）。