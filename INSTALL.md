# Novel Engine 安装与快速上手

> 面向第一次拿到本技能包的人。如果你是作者本人，忽略本文档，直接看 `README.md`。
> 本技能包 = 纯 Markdown 写作状态机（主）+ Python 检索运行时（`runtime/`，可选）。

---

## 1. 这是什么

一个给 **AI 写作助手** 用的"技能包"：你把整个目录装进助手，它就能按一套工业级流程帮你写网络小说。支持两种创作模式：

| 模式 | 触发指令 | 特点 |
|------|---------|------|
| **长篇小说（五阶段 SOP）** | "开新书" / "写第 1 章" | 章节状态管理、反 AI 味、素材池、检索，适合连载 |
| **短故事单篇** | "写个短故事" / "短篇" | 一次成篇，不建项目，适合推文风/抖音短篇 |

核心原则：**技能只存模板，项目存内容**（你的小说正文存在独立项目目录，技能本身不碰）。

---

## 2. 目录结构总览

```
novel-engine/
├── SKILL.md              ← 技能入口（助手读取，自动识别）
├── README.md             ← 设计文档（作者视角）
├── INSTALL.md            ← 本文件（安装/上手）
├── routes/index.md       ← 路由台账：所有文件的"地图"（唯一真相源）
├── references/           ← 五阶段方法库（规划/写作/自检/反AI味/落库…）+ 短故事（short-story.md）
├── library/              ← 用户级素材库（题材风格包/技巧/平台/身份…）
├── templates/            ← 模板（配置/章节/角色/素材池/短故事骨架…）
├── runtime/              ← Python 检索运行时（可选，见 §3）
└── story/                ← 项目结构示意（仅示意，实际项目另建）
```

- **装好即用**：`SKILL.md` + `references/` + `templates/` + `library/` + `routes/` 都是只读的成品。
- **可扩展**：`library/` 里可放入你自己积累的题材包/技巧。
- **运行时生成**：`runtime/` 会在你建项目时自动往**项目目录**写索引（如 `故事/索引/bm25/`），不影响技能本体。

---

## 3. 安装步骤

### 3.1 环境要求

- 一个支持"技能（Skill）"功能的 AI 助手客户端（豆包等）。
- **Python 3.8+**（仅"检索"功能需要；纯写作不装也能用）。
- 安装 Python 依赖（可选，检索用）：

```bash
pip install -r runtime/requirements.txt
```

> 依赖说明：`jieba`（中文分词，必装）· `pyyaml`（配置读取）· `numpy`（向量）。`zvec`、`pypinyin` 是 **ZVEC 语义检索可选增强**的依赖，不装则自动降级为纯 BM25 词法检索，功能不中断。

### 3.2 安装为技能

1. 把**整个 `novel-engine/` 目录**复制到你助手的"技能目录"（例如豆包的 `.user_skills/novel-engine/`）。
2. 保持目录名与 `SKILL.md` 顶部的 `name: novel-engine` 一致。
3. 重启助手 / 刷新技能列表，让它识别到新技能。

> 不同助手的技能目录位置不同，请查你所用助手的"技能/插件"设置页。

### 3.3 验证安装

对你的助手说：

```
写个短故事
```

如果能进入短故事模式（不建项目、直接要脑洞），说明安装成功。再试：

```
开新书
```

进入长篇小说初始化流程。

---

## 4. 快速上手（5 分钟跑通）

### 模式一：长篇小说

1. 对助手说 **"开新书"** → 进入初始化（设置书名/字数/题材）。
2. （推荐）提供 1-3 本对标书 → 助手自动拆解建素材池（两阶段）。
3. 说 **"写第 1 章"** → 五阶段 SOP：规划 → 写作 → 自检 → 反 AI 味 → 落库。
4. 新角色登场时，助手自动建档；每章结束自动更新状态与索引。

### 模式二：短故事单篇

1. 对助手说 **"写个短故事"**（或"抖音推文风"）。
2. 给一句脑洞（主角 + 困境 + 目标）。
3. 助手按"导语 → 憋屈 → 反转 → 断章"四幕一次成篇，附 Anti-AI 过检。详见 `references/short-story.md`。

---

## 5. 常见问题 FAQ

**Q：不装 Python 能用吗？**
能。纯写作流程不依赖 Python；只有"检索"（写前查状态/落库建索引）需要。不装则跳过检索，其余功能照常。

**Q：`verify.py` / `doc_sync.py` 是干嘛的？报错要紧吗？**
- `verify.py` 是**流程门禁**：助手每轮交付前自动检查项目完整性，报错 = 流程未走完，助手应停下修正而非硬交付。
- `doc_sync.py` 是**说明文档同步**：修改技能文件后自动刷新各目录 `_README.md` 清单。这俩是给助手/维护者用的，你无需手动运行。

**Q：ZVEC 是什么？必须装吗？**
可选增强。ZVEC 是语义检索（能查到"同义改写"的内容），比默认 BM25 词法检索更聪明，但要额外装 SDK、索引更大。不装就自动用 BM25，功能不中断。

**Q：为什么有的文件名带 `故事/真相`、`故事/元数据` 这种中文路径？**
技能约定项目目录**统一中文命名**（协议文件名保持英文，如 `chapter_0001.md`）。项目建在技能目录之外，与技能本体隔离。

**Q：短故事和长篇能混用吗？**
能。它们走两套流程，互不干扰。短故事不建项目，长篇用项目状态管理。

**Q：遇到问题去哪看？**
- 助手行为异常 → 看 `routes/index.md`（文件地图）+ `SKILL.md`（三铁律/五阶段）。
- 想了解设计理念 → 看 `README.md`。
- 想排查检索 → 看 `runtime/bm25_fts.py` 头部的用法说明与 `references/retrieval.md`。

---

## 6. 进阶

- **拆书/素材池**：见 `templates/pools/拆解方法论.md` 与 `references/project-setup.md`。
- **反 AI 味**：见 `references/anti-ai.md`（写前防御 + 写后清洗）。
- **外包写作**：说"外包写作"，助手会打包一份自包含提示词，你贴到网页 chat 写完后贴回，流程自动接反 AI 味与落库。
