> 全程参考 · 5阶段SOP交互详解 · 路由见 `routes/index.md`

# 5 阶段 SOP 交互详解

## 流程总览

```
用户："写第X章"
    │
    ▼
[阶段① 规划] 读取 故事/真相/4文件 + session-start.md
    │ （状态冲突按5级优先级裁决）
    │ （按需加载 素材池/ 素材 + techniques 技巧）
    │ 输出：规划清单
    ▼
[阶段② 写作] 读取 writing-guide.md
    │ （按需加载 素材池/reference/samples/ 同类场景样本）
    │ 输出：正文初稿（无 YAML；元数据写 故事/元数据/chapter_XXXX.md，功能三）
    ▼
[阶段③ 自检] 读取 self-review.md
    │ 4问行为验证检测（不是"自夸"，是检测行为模式）
    │ 最多 3 轮修正 + Best Version 保留
    │ 输出：自检报告 + 最终正文版本
    ▼
[阶段④ 润色] 读取 anti-ai.md
    │ 对照式转换：AI版 → 转换过程 → 目标版
    │ 输出：精修稿
    ▼
[阶段⑤ 落库] 读取 hooks-and-memory.md + change-report-spec.md
    │ 输出：生成 Change Report → 更新 故事/真相/4文件 → 保存正文
    ▼
完成，等待用户下一指令
```

## 阶段间交互规范

- 每个阶段完成后，向用户输出该阶段的产出物
- 用户可以喊停修改，修改后从当前阶段继续
- 不要把多个阶段的结果合在一起一次性输出（防止跳过自检）
- 如果用户说"直接写不要规划"，可以跳过阶段①，但必须在正文末尾附上 Frontmatter

## 阶段① 规划交互

```
Agent 读取（强制）：
  故事/真相/characters.md — 基线摘要 + 当前状态
  故事/真相/world.md — 核心规则 + 当前状态
  故事/真相/hooks.md — 伏笔追踪
  故事/真相/relationships.md — 关系网络
  novel-config.json — 项目配置
  上一章 Frontmatter — change_summary + emotion_arc

Agent 读取（按需）：
  素材池/author_dna/ — 风格校准
  素材池/planner/ — 节奏校准
  素材池/reference/techniques/ — 对应技巧方法论
  素材池/reference/samples/ — 同类场景样本
  设定/角色声线.md — 仅新角色或声线存疑

Agent 内部判断：
  状态冲突？→ 按5级优先级裁决（世界规则 > 硬边界 > 核心性格 > 章态 > 剧情需求）

Agent 输出：规划清单（按 session-start.md 模板）
用户确认或修改
```

如果用户说"按你说的来"或"继续"，直接进入阶段②。

## 阶段② 写作交互

```
Agent 读取：writing-guide.md（内化写作原则）
Agent 执行：
  1. 有对话戏 → 先在脑中完成嘴心5问
  2. 按规划清单写作
  3. 写完后元数据写入 故事/元数据/chapter_XXXX.md（含 change_summary + review_status，正文无 YAML）
Agent 输出：完整正文初稿
```

写作时不解释"我在用什么技巧"，直接输出正文。

## 阶段③ 自检交互

```
Agent 读取：self-review.md
Agent 执行：4 问行为验证检测
  Q1：章末悬念行为检测
  Q2：声线行为检测（对照 characters.md 基线摘要）
  Q3：三大AI味模式行为检测（计数命中数）
  Q4：感官行为检测
  每问必须标注 evidence_ids（原文片段）
Agent 输出：自检报告
```

### 修正循环（最多 3 轮）

```
第1轮自检 → 通过 → 阶段④
        ↘ 未通过 → 修正（只改命中项，不全面重写）→ 第2轮自检
                                        ↘ 通过 → 阶段④
                                        ↘ 未通过 → 再修正 → 第3轮自检
                                                          ↘ 通过 → 阶段④
                                                          ↘ 未通过 → 停止，输出 Best Version
```

### Best Version 保留机制

每轮自检后打一个综合分（1-10，粗略即可），3轮后输出**分数最高的版本**，不一定是第3轮。越改越差时回退到之前的最佳版本。

### review_status 标记

最终 Frontmatter 的 `review_status` 字段：
- `full_pass` — 4/4 全部通过
- `partial_pass` — 3/4 通过，剩余 1 项有瑕疵但可放行
- `needs_human_review` — ≤2/4 通过，需要作者重点审稿

## 阶段④ 润色交互

```
Agent 读取：anti-ai.md
Agent 执行：
  1. 逐段扫描 3 大检测模式（乒乓球短句/段末升华/解释腔）
  2. 命中 → 对照四大转换原则（心理→动作/解释→结果/对称→不对称/总结→落物）
  3. 参考对照示范的转换过程，最小修改
  4. 不命中 → 不改
Agent 输出：精修稿（完整正文，不是 diff）
```

润色后简要说明改了什么（命中了几处、改了哪类问题），不要输出完整 diff。

## 阶段⑤ 落库交互

```
Agent 读取：hooks-and-memory.md + change-report-spec.md
Agent 执行：
  1. 生成 Change Report（角色/伏笔/时间线/世界规则/关系变化）
  2. 根据 Change Report 更新 故事/真相/ 下 6 个状态文件（含 objects/timeline）
  3. 保存正文文件（含 Frontmatter + review_status）
Agent 输出：落库报告
```

落库后提示用户下一章的伏笔债务状态。

---

## 记忆分层读取规则（job-0027补强 · 防止上下文溢出）

> 来源：网文连载完整工作流。novel-engine 已有"写前必读铁律"（6状态文件），本节补充**分层读取策略**，避免每次全量加载导致溢出。

### 4层记忆架构

| 层级 | 文件 | 何时读 | 内容粒度 |
|------|------|--------|---------|
| **核心层** | 故事/真相/6文件 + index + 上一章 | **每次必读** | 最新、最短，保证不乱接 |
| **扩展层** | 人物台词卡 + writing-guide | **写正文时读** | 声线规范 + 写作原则 |
| **检索层** | hooks.md（伏笔） | **查旧线索时按关键词检索** | 长期档案，不每次全读 |
| **静态层** | anti-ai.md + techniques/*.md | **按需加载** | 参考文档，不入工作记忆 |

### 章后固定动作（job-0027补强）

每写完一章后必须执行（阶段⑤落库前）：

1. ✅ 保存章节正文
2. ✅ **台词复核 + 遮名测试**（T-014七项检查）
3. ✅ **硬禁扫描**（anti-ai-词级信号.md A/B/C/D级）
4. ✅ 统计有效正文字数
5. ✅ 更新 index.md（当前进度 + 最近三章摘要）
6. ✅ 追加 hooks.md（伏笔新增/回收追踪）
7. ✅ 若人物状态变化 → 更新 characters.md
8. ✅ 若角色话术出现新稳定特征 → 更新声线配置
9. ✅ 展示新章节给用户
10. ✅ **给用户5-8条本章简纲**（承接/行动/冲突/转折/收获/关系变化/章尾钩子）

### 交付物规范（job-0027补强）

每章交付必须包含：
- 章节正文文件
- 有效正文字数
- 5-8条简纲
- 硬禁扫描结果（A/B/C/D级命中情况）
- **台词复核结果**（已检查某几位角色的对白差异）

## 新人误区自检表（job-0033 · 龙空论坛补充）

> 来源：龙空论坛高手问答；写正文前必读，避免自嗨陷阱

| # | 误区 | 自检问题 | 修复方向 |
|---|-----|---------|---------|
| 1 | **自嗨陷阱** | 我写的这段，是我自己觉得爽，还是读者会觉得爽？ | 以读者心态重写，不要代入作者视角 |
| 2 | **细节过载** | 这段细节描写是否阻碍了节奏？是否写了两遍同样的信息？ | 删除冗余描写，只保留推动情节的细节 |
| 3 | **配角抢戏** | 配角是否比主角更耀眼？读者会代入谁？ | 降调配角，让主角成为全场焦点 |
| 4 | **速度陷阱** | 前三章细节描写是否太重，导致后面越来越水？ | 前三章保持快节奏，不要沉溺细节 |
| 5 | **拒绝反馈** | 读者/编辑提的意见，我是否完全不听？ | 开放心态，区分"建议"和"批评" |
| 6 | **同质化** | 这个爽点/桥段，是否网上已经有100本书写过？ | 加入自己的稀缺性（角色/方法/世界观差异） |

> **核心原则**：写读者想看的，不是写自己想写的。

### 打补丁操作表（job-0039 · 工程层）

> 场景切换/时间切换/人物切换时容易产生漏洞，需要打补丁。

| 漏洞类型 | 表现 | 补丁方法 |
|---------|------|----------|
| **时间混乱** | 穿越/重生多时间穿插，读者混乱 | 单独列一章楔子；或重整时间顺序 |
| **时间跨度太长** | 重生成小孩童年太幼稚 | 十七八岁成年入手；跳屏"已修炼十六年" |
| **场景切换突然** | 双线场景切换太突兀 | 分割线（歪招）；共同点绝招（物品/人物/心理衔接） |

> **共同点衔接绝招**：在上场景引入某物/某人/某心理 → 下场景立即承接同一个点，自然过渡。

### 商业写作硬规则表（job-0042 · 16条）

> 来源：小白文商业写作；前10章签约公式 + 硬性数字约束。

| # | 规则 | 数字 |
|---|-----|------|
| 1 | 主角必须及早登场 | 300字内 |
| 2 | 第一章爆发第一个矛盾冲突 | 3000字内 |
| 3 | 亮出功法体系或最大卖点 | 3章内 |
| 4 | 爆发第一个高潮 | 3万字内 |
| 5 | 高潮后展开支线 | 至少3条 |
| 6 | 解决第一个大情节 | 30万字前后（配合上架） |
| 7 | 每次高潮获得战利品 | 3件（马上用/后续情节/送人） |
| 8 | 主角身边异性 | 至少3个 |
| 9 | 铺垫与打斗比例 | 七分铺垫三分打斗 |
| 10 | 同一桥段出现上限 | 不超过3次 |
| 11 | 每章场景切换上限 | 不超过3个场景 |
| 12 | 每章内容比例 | 对话3成+叙述3成+描写3成 |
| 13 | 旁白/内涵上限 | 每章不超过300字 |
| 14 | 视角混用禁止 | 禁止3种以上视角混合 |
| 15 | 主角打酱油上限 | 每3000字检查一次 |
| 16 | 通篇打斗禁止 | 禁止连续3章通篇打斗 |

## 中断与恢复

如果对话中途断开（compact 或新会话）：
1. 读取最近章节的 Frontmatter 恢复上下文
2. 读取 故事/真相/ 6个状态文件（含 objects/timeline）确认当前状态
3. 检查 hooks.md 确认伏笔状态
4. 如果有未完成的自检（阶段③未完成），重新自检
5. 如果正文已落库但自检未做，补做自检后再继续
6. 如果 Best Version 记录丢失，按当前版本继续，不回溯

## 并行写作

不支持。一次只写一章，一章只走一遍完整 SOP。
